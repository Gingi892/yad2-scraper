name: Yad2 Scraper

on:
  schedule:
    - cron: '*/15 5-23 * * *'  # כל 15 דקות בין 05:00–23:00
  workflow_dispatch:
    branches:
      - '**'

jobs:
  scraper:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install packages
        run: yarn install

      - name: Setup git config
        run: |
          git config user.name "GitHub Actions"
          git config user.email "${{ secrets.GIT_CONFIG_EMAIL }}"

      - name: Run scraper (Telegram + WhatsApp)
        run: |
          export API_TOKEN=${{ secrets.API_TOKEN }}
          export CHAT_ID=${{ secrets.CHAT_ID }}
          export WHATSAPP_TOKEN=${{ secrets.WHATSAPP_TOKEN }}
          export PHONE_NUMBER_ID=${{ secrets.PHONE_NUMBER_ID }}
          export RECIPIENT_PHONE=${{ secrets.RECIPIENT_PHONE }}
          yarn scrape

      - name: Push new json data if needed
        run: |
          if [ -f ./push_me ]; then
            echo Pushing to Github...
            git add .
            DATE=$(date +"%F, %H:%M:%S")
            git commit -m "updated data - $DATE"
            git push
          fi
